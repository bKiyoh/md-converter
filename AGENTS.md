# AGENTS.md

## プロジェクト概要

このリポジトリでは、Markdownで作成した文章をSlack、Backlog、プレーンテキストなどの形式へ変換するWebアプリを開発する。

入力内容と変換処理はブラウザ内で完結させる。
MVPではバックエンド、ユーザー登録、外部API連携を実装しない。

---

## 技術構成

- Vue 3
- TypeScript
- Vite
- Composition API
- Vitest
- ESLint
- npm

状態管理は、必要性が明確になるまではPiniaを導入せず、Composableとコンポーネントの状態で管理する。

MVPの入力欄は通常の`textarea`で実装する。
CodeMirrorやMonaco Editorは初期実装では導入しない。

---

## 実装方針

- Markdown文字列を正規表現だけで直接変換しない
- Markdownを一度構文解析し、中間表現またはASTを経由して変換する
- 出力形式ごとにConverterを分離する
- UIと変換ロジックを分離する
- 変換ロジックはVueコンポーネントへ直接記述しない
- コードブロック内のMarkdown記号を誤変換しない
- 入力内容を外部サーバーへ送信しない
- プレビューにHTMLを使用する場合はサニタイズする
- 不要な抽象化や将来機能の先行実装を避ける
- 同じ責務の処理を複数箇所へ重複して実装しない
- 既存の共通型やインターフェースを優先して使用する

---

## ディレクトリ方針

```text
src/
├─ components/
│  ├─ editor/
│  ├─ output/
│  └─ common/
├─ composables/
├─ converters/
│  ├─ slack/
│  ├─ backlog-markdown/
│  ├─ backlog-notation/
│  └─ plain-text/
├─ parser/
├─ types/
└─ utils/
```

実際の構成は、実装上の合理的な理由がある場合に変更してよい。
変更した場合は、その理由と影響範囲を説明すること。

---

## 対応する出力形式

MVPでは以下に対応する。

- Slack
- Backlog Markdown
- Backlog記法
- プレーンテキスト

標準Markdown出力は、入力内容の確認・正規化用途として扱う。

---

## MVPで対応するMarkdown要素

- 見出し
- 段落
- 太字
- 斜体
- 打ち消し線
- 箇条書き
- 番号付きリスト
- チェックリスト
- 引用
- インラインコード
- コードブロック
- リンク
- テーブル
- 水平線
- 改行

---

## MVP対象外

以下は実装しない。

- ログイン
- サーバー保存
- クラウド同期
- 共同編集
- Slack APIへの直接投稿
- Backlog APIへの直接登録
- 画像アップロード
- AI文章生成
- 有料プラン
- 高機能Markdownエディタ

---

## 仕様書

作業開始前に、以下のファイルを確認すること。

- `AGENTS.md`
- `docs/product-spec.md`
- `docs/conversion-rules.md`
- `docs/implementation-plan.md`
- `docs/tab-management-spec.md`

各ファイルの役割は以下のとおり。

| ファイル                      | 役割                             |
| ----------------------------- | -------------------------------- |
| `AGENTS.md`                   | 開発全体で守るルール             |
| `docs/product-spec.md`        | 画面、機能、MVP範囲              |
| `docs/conversion-rules.md`    | Markdownから各形式への変換仕様   |
| `docs/implementation-plan.md` | 技術設計、実装順、マイルストーン |
| `docs/tab-management-spec.md` | Markdownタブ管理、削除、保存仕様 |

仕様書間に矛盾がある場合は、推測で解決せず報告すること。

---

## 変換仕様の確認

- `docs/conversion-rules.md` の記法を無条件に正しいものとして扱わない
- SlackおよびBacklog固有の記法は、実装前に公式仕様と照合する
- 公式仕様を確認できない項目は、推測で実装しない
- 公式仕様と変換ルールに差異がある場合は、実装前に報告する
- 変換先で表現できない要素は、以下の優先順位で扱う

1. 意味を維持できる代替表現へ変換する
2. プレーンテキストへ変換する
3. 変換結果へ警告を追加する

---

## コーディング規約

- TypeScriptの型を明示する
- `any`を安易に使用しない
- コンポーネントを過度に巨大化させない
- 関数名は処理内容が分かる名前にする
- コメントは処理内容ではなく、判断理由を説明する場合に使用する
- 既存コードの規約がある場合は、既存規約を優先する
- 依存パッケージを追加する場合は、目的と選定理由を説明する
- 不要な依存関係を追加しない
- 将来利用するかもしれないという理由だけで機能や抽象化を追加しない

---

## テスト方針

変換処理には単体テストを作成する。

最低限、以下を確認する。

- 通常の変換
- 空文字
- 複数要素の組み合わせ
- ネストしたリスト
- コードブロック内の記号
- 変換先で表現できない要素
- 日本語を含むテーブル
- 改行
- 不正または不完全なMarkdown

変換仕様を変更した場合は、対応するテストも更新する。

テストでは、実装内部の細かな処理ではなく、入力と出力、および警告内容を確認する。

---

## 開発ワークフロー

実装は、`docs/implementation-plan.md` に定義されたマイルストーン単位で行う。

一度に複数のマイルストーンを実装しない。

基本的な進め方は以下のとおり。

1. 対象マイルストーンを確認する
2. 前のマイルストーンまでの実装内容を確認する
3. 対象マイルストーンの範囲のみ実装する
4. lint、型チェック、テスト、必要に応じてビルドを実行する
5. 手動確認が必要な操作を整理する
6. 実装内容と検証結果を報告する
7. 問題がある場合は、同じマイルストーン内で修正する
8. 次のマイルストーンを先行実装しない

---

## マイルストーン実施時の共通ルール

- 指示されたマイルストーン以外の機能を先行実装しない
- 前のマイルストーンで作成した設計やインターフェースを尊重する
- 既存コードを確認してから変更する
- 同じ責務の処理を重複して作成しない
- 不要な依存関係を追加しない
- MVP対象外の機能を追加しない
- 仕様を勝手に拡張しない
- 仕様や実装計画に矛盾がある場合は、推測で進めず報告する
- 変換仕様を変更した場合は、対応するテストも更新する
- 実行できなかった検証は、成功したものとして扱わない
- 対象マイルストーンの完了条件は、`docs/implementation-plan.md` に従う

---

## マイルストーン別の注意事項

### Milestone 1：プロジェクト初期構成

- Vue 3、TypeScript、Viteの初期構成を作成する
- ESLint、Vitest、型チェックを実行できる状態にする
- 不要なUIライブラリや状態管理ライブラリを導入しない
- サンプルテストを1件以上作成する
- READMEに起動、lint、型チェック、テスト、ビルドの手順を記載する
- 変換機能は実装しない

### Milestone 2：Markdown解析と共通型

- Markdown解析結果を確認できる単体テストを追加する
- Vueコンポーネントには変換処理を実装しない
- 後続のConverterが利用できる共通型を定義する
- GFMのテーブルとチェックリストを扱えることを確認する
- コードブロック内のMarkdown記号が通常要素として解析されないことを確認する

### Milestone 3：Slack変換

- Slack変換のみ実装する
- Backlog変換や画面実装は行わない
- 見出し、強調、リンク、リスト、引用、コード、テーブルをテストする
- 変換先で表現できない要素は警告として返す
- Markdown文字列全体を正規表現だけで変換しない

### Milestone 4：Backlog Markdown変換

- Backlog Markdown向けの変換のみ実装する
- Backlog記法とは明確に分離する
- Markdownと同じ記法についても、Backlog上での挙動を確認する
- 公式仕様を確認できない項目は推測で実装しない

### Milestone 5：Backlog記法変換

- Backlog記法向けの変換のみ実装する
- Backlog Markdownと混同しない
- `docs/conversion-rules.md` と公式仕様に差異がある場合は報告する
- 未確認のBacklog固有記法は実装しない

### Milestone 6：プレーンテキスト変換

- Markdown記号を単純に削除するだけの実装にしない
- 見出し、リスト、リンクなどの文章構造を維持する
- 読みやすい日本語テキストとして出力する
- テーブルの内容が失われないことを確認する

### Milestone 7：エディタ画面

- 変換ロジックをVueコンポーネントへ移動しない
- 既存のParserとConverterを呼び出す構成にする
- Markdown入力欄、出力形式選択、変換結果、文字数を実装する
- MVP対象外の高機能Markdownエディタを導入しない
- バックエンドや外部API連携を追加しない

### Milestone 8：コピー、自動保存、警告表示

- LocalStorageへの保存対象とキーを明確にする
- 保存処理はデバウンスする
- ページ再読み込み後に入力内容を復元する
- コピー成功時と失敗時の表示を実装する
- Converterが返した警告を画面に表示する
- 入力内容を外部サーバーへ送信しない

### Milestone 9：レスポンシブ対応と最終テスト

- 新機能を追加せず、既存仕様の完成度を上げる
- PCと狭い画面幅の両方を確認する
- 未使用コード、未使用依存関係、不要な設定を確認する
- 全単体テスト、lint、型チェック、ビルドを実行する
- MVP完了条件を項目ごとに確認する
- 実行できなかった確認は残課題として明記する

### Milestone 15：Markdownタブ管理とデータ保存

- Markdown入力を最大7件のタブとして管理する
- タブ管理と保存処理をComposableへ分離する
- 既存の単一文書データを初期タブへ移行する
- タブ追加、切り替え、名前変更、削除、復元、完全削除を実装する
- 削除済みタブを30日間保持し、期限切れデータを起動時に削除する
- 独自の編集履歴、複数ウィンドウ同期、競合解決は実装しない
- 詳細な挙動と保存形式は `docs/tab-management-spec.md` に従う

---

## 作業時のルール

- 作業開始前に関連する仕様書を読む
- 大きな変更は実装前に計画を提示する
- 指示されたマイルストーン以外を先行実装しない
- 仕様に曖昧な点がある場合は、勝手に機能を増やさず仮定を明示する
- 既存の動作を変更する場合は、影響範囲を説明する
- 実装計画の変更が必要な場合は、コードより先に`docs/implementation-plan.md`を更新する
- 仕様変更が必要な場合は、該当する仕様書を更新してから実装する
- 実装後にlint、型チェック、単体テストを実行する
- 必要に応じてビルドを実行する
- 実行できなかった検証がある場合は、その内容と理由を報告する

---

## 検証

実装後は、`package.json` に定義されたスクリプトを確認し、原則として以下を実行する。

```bash
npm run lint
npm run type-check
npm run test
npm run build
```

実際のスクリプト名が異なる場合は、`package.json`に定義されたコマンドを使用する。

マイルストーンの内容によって実行不要なコマンドがある場合は、実行しなかった理由を報告する。

コマンドを実行できなかった場合や失敗した場合は、その状態を明記し、成功したものとして報告しない。

---

## 実装計画を変更する場合

実装中に設計やマイルストーンの変更が必要になった場合は、コードだけを先に変更しない。

以下の順番で対応する。

1. 変更が必要な理由を整理する
2. `docs/implementation-plan.md`を更新する
3. 影響する仕様書を確認する
4. 必要に応じて`docs/product-spec.md`または`docs/conversion-rules.md`を更新する
5. 更新後の計画に基づいて実装する

仕様変更と実装変更を同じものとして扱わない。

---

## 完了時の報告

作業完了時は以下を報告する。

1. 実装した内容
2. 変更した主要ファイル
3. 採用した設計
4. 追加・変更した依存関係と理由
5. 追加・更新したテスト
6. 実行したコマンド
7. 各コマンドの結果
8. 手動で確認すべき操作
9. 残っている課題
10. 次のマイルストーンへ持ち越す内容

報告の最後に、作業内容に適した推奨コミットメッセージを1件、コードブロックで記載する。
