# Md Converter 完了済み実装計画（履歴）

> この文書は完了したマイルストーンの計画と判断経緯を保存する履歴資料です。
> 現行の実装計画は [`implementation-plan.md`](../plans/implementation-plan.md)、現行仕様への
> 入口は [`README.md`](../README.md) を参照してください。この文書は現行仕様の正本ではありません。

## 0. この計画の前提と対象範囲

本計画は `AGENTS.md`、`docs/product-spec.md`、`docs/conversion-rules.md`、`docs/tab-management-spec.md`、`docs/focus-mode-spec.md` と、2026-07-28時点のリポジトリ状態を基にする。Markdown入力と変換をブラウザ内だけで完結させ、バックエンド、ユーザー登録、外部API連携、高機能エディタなどの対象外機能は実装しない。複数文書管理はMilestone 15、PC表示の左右ペイン幅調整はMilestone 16、選択中文書の編集へ集中するフォーカスモードはMilestone 17、通常モードの入力支援表示はMilestone 18、Markdownタブのドラッグ並べ替えはMilestone 20、選択中本文の文章検索・一括置換はMilestone 21、検索一致箇所のハイライトはMilestone 22、入力置換はMilestone 23、設定で有効化する全角Markdown補正はMilestone 24で追加する。

変換ルール文書には未定義の要素や、Slack・Backlogの公式仕様との照合が必要な記法が残っている。この計画ではそれらを推測で確定せず、「仕様上の要確認事項」として管理する。確認前に実装が必要になった場合は、マイルストーン開始時に仮定と影響範囲を提示してレビューを受ける。

## 1. 現在のリポジトリ構成

現時点ではVueアプリのソース、Vite設定、テスト、lint設定、依存パッケージはまだ存在しない。`README.md`、`package.json`、`docs/implementation-plan.md` は空であり、実質的には仕様策定段階である。

```text
md-converter/
├─ .vscode/
│  └─ settings.json          # Nuxtプロジェクトではないことを示すIDE設定
├─ docs/
│  ├─ conversion-rules.md    # 一部要素の変換例。未定義項目あり
│  ├─ implementation-plan.md # 本計画
│  └─ product-spec.md        # MVPの画面・機能・完了条件
├─ AGENTS.md                 # 技術方針、対象範囲、作業ルール
├─ README.md                 # 空ファイル
└─ package.json              # 空ファイル
```

Git上では上記ファイルがすべて未追跡で、コミット履歴も確認できない。Milestone 1では既存の仕様文書を保持したまま、Vue 3 + TypeScript + Viteの最小構成を追加する。

## 2. 採用する技術構成

| 分類 | 採用候補 | 用途・方針 |
| --- | --- | --- |
| UI | Vue 3 / Composition API | 画面と局所的な状態を管理する |
| 言語 | TypeScript | AST正規化、変換結果、警告を明示的に型付けする |
| ビルド | Vite | 開発サーバーと本番ビルドを提供する |
| Markdown解析 | `unified` + `remark-parse` + `remark-gfm` | Markdownをmdastへ解析し、GFMのテーブル・タスクリスト・打ち消し線を扱う |
| テスト | Vitest + Vue Test Utils | 変換ロジックの単体テストを中心に、必要最小限のコンポーネントテストを行う |
| 静的検査 | ESLint（Vue/TypeScript対応） | 型チェックとは別にコード品質を検査する |
| 型チェック | `vue-tsc` | `.vue` を含むTypeScriptの型を検査する |
| パッケージ管理 | npm | `package-lock.json` で依存を固定する |
| ブラウザ保存 | Web Storage API (`localStorage`) | 最大7文書のタブ状態、削除済みタブ、テーマ選択、変換形式、表示設定を端末内に保存する |
| コピー | Clipboard API | ユーザー操作時に変換結果をコピーし、成功・失敗を通知する |

Pinia、Vue Router、UIコンポーネントライブラリ、CodeMirror、Monaco EditorはMVPでは導入しない。状態はルート画面とComposableで十分に管理できるためである。HTMLプレビューは既存のMarkdown解析結果から許可した要素だけをレンダリングし、生HTMLを破棄する。テキスト・属性をエスケープし、リンクURLのスキームを制限するため、HTMLサニタイザー依存は追加しない。

依存パッケージの正確なバージョンとNode.js対応範囲は、Milestone 1開始時に各パッケージの公式情報を確認して確定する。

## 3. Markdownパーサーの候補と選定理由

### 候補A：unified / remark（採用候補）

- `remark-parse` がMarkdownをmdastへ変換し、文字列置換ではなく構文木を基準に処理できる。
- `remark-gfm` により、MVP対象のテーブル、タスクリスト、打ち消し線を同じAST上で扱える。
- ブロック要素とインライン要素をノードとして区別でき、コードブロック内のMarkdown記号を誤変換しにくい。
- mdastの型情報を利用でき、アプリ用中間表現への変換境界を明確にできる。
- unifiedの出力プラグインを形式ごとに増やすのではなく、解析だけに利用する。これによりSlackやBacklog固有の変換方針をアプリ側で明示できる。

注意点は、`remark-gfm` を含む関連パッケージの構成とESM対応、改行ノードの扱い、CommonMark/GFMと各貼り付け先の差を事前に検証する必要があることである。

### 候補B：markdown-it

- CommonMark寄りのパーサーで、トークン列を取得でき、拡張も豊富である。
- 一方、MVPで必要な入れ子構造をアプリ用の木構造へ正規化する際、開始・終了トークンから階層を再構築する処理が必要になる。
- 今回は複数形式へ同一構造を変換するため、初めから木構造を得られるmdastの方がConverter間で共有しやすい。

### 候補C：micromarkを直接利用

- CommonMark/GFMの構文解析基盤として制御性は高い。
- ただし低レベルであり、MVPに必要なAST構築や変換処理の実装量が増える。独自Markdown方言を解析する要件もないため、現段階では過剰である。

以上から、Milestone 2で小さな技術検証を行った上で `unified` + `remark-parse` + `remark-gfm` を採用する。検証でMVP要素のASTが期待どおり得られない場合にだけ、markdown-itを再評価する。

## 4. Markdown入力から各形式への変換フロー

```text
textareaのMarkdown文字列
        │
        ▼
parseMarkdown(input)
  remark-parse + remark-gfm
        │ mdast
        ▼
normalizeMarkdownAst(tree)
  対応ノードをアプリ用中間表現へ変換
  未対応・不完全な構文を警告候補として収集
        │ MarkdownDocument
        ▼
選択中のConverter.convert(document)
  ├─ SlackConverter
  ├─ BacklogMarkdownConverter
  ├─ BacklogNotationConverter
  └─ PlainTextConverter
        │ ConversionResult
        ├─ output: string
        └─ warnings: ConversionWarning[]
        ▼
出力表示 / 警告表示 / クリップボードへのコピー
```

- 選択中タブの入力変更のたびに解析・変換する。textareaは1つだけを表示し、タブ切り替え時に対象文書を差し替える。
- パースに失敗して画面全体が停止しないよう、UI境界で例外を捕捉し、入力は保持したまま変換不能の警告を表示する。
- 入力の自動保存は変換フローと分離し、約500msのデバウンス後に元のMarkdown文字列を保存する。
- 変換形式を切り替えた場合は、既に正規化した文書を選択先Converterへ渡す。早すぎるキャッシュ最適化は行わず、性能上必要になった場合のみ `computed` で再計算範囲を制御する。
- 空文字は正常入力として扱い、空の出力と警告なしを返す。

## 5. ASTまたは中間表現の扱い

remarkのmdastをConverterへ直接公開せず、MVP要素だけを表すアプリ用の `MarkdownDocument` に一度正規化する。

理由は次のとおりである。

- パーサー固有の型や拡張ノードをConverterから隔離できる。
- GFMタスクリストなどのパーサー表現を一度だけ解釈し、全Converterで共通利用できる。
- 表現できない要素の警告規則を形式ごとに実装しやすい。
- Converterのテストではパーサーを通さず、必要な構造だけを組み立てて検証できる。

一方、将来要件を見越した汎用ASTは作らない。MVPで必要な最小限の型だけを定義する。

想定する概念は以下である。実際のTypeScript定義はMilestone 2でテストと共に確定する。

```ts
type MarkdownDocument = {
  blocks: BlockNode[]
}

type BlockNode =
  | HeadingNode
  | ParagraphNode
  | ListNode
  | QuoteNode
  | CodeBlockNode
  | TableNode
  | ThematicBreakNode

type InlineNode =
  | TextNode
  | StrongNode
  | EmphasisNode
  | DeleteNode
  | InlineCodeNode
  | LinkNode
  | LineBreakNode
```

補足方針：

- 見出しはレベルを保持する。
- 箇条書き・番号付きリストは `ordered`、開始番号、入れ子の項目を保持する。
- チェック状態はリスト項目の `checked: boolean | null` として保持する。
- コードブロックは本文と言語情報を保持し、子のMarkdownとして再解釈しない。
- テーブルはヘッダー、行、セル内のインライン要素、配置情報を必要な範囲で保持する。
- ソフト改行と明示的な改行を区別できる情報を保持する。最終的な出力規則は仕様確認後に決める。
- 位置情報は警告箇所の表示に必要な開始行・列へ絞って引き継ぐ。詳細なソースマップは作らない。
- パーサーが通常テキストとして解釈した不完全なMarkdownは、機械的に「不正」と断定できないため、警告対象にする条件をテストで限定する。

## 6. Converterのインターフェース

MVPではクラス階層やDIコンテナを導入せず、次の小さなインターフェースを満たすオブジェクトまたは関数群として形式ごとに実装する。

```ts
type OutputFormat =
  | 'slack'
  | 'backlog-markdown'
  | 'backlog-notation'
  | 'plain-text'

type WarningCode =
  | 'unsupported-node'
  | 'lossy-conversion'
  | 'invalid-structure'

type ConversionWarning = {
  code: WarningCode
  message: string
  location?: {
    line: number
    column: number
  }
}

type ConversionResult = {
  output: string
  warnings: ConversionWarning[]
}

interface Converter {
  readonly format: OutputFormat
  convert(document: MarkdownDocument): ConversionResult
}
```

- `convert` は同じ入力に対して同じ結果を返す副作用のない処理とする。
- ConverterはDOM、Vue、LocalStorage、Clipboard APIを参照しない。
- 警告は「出力先で表現を保持できず、情報が欠落または代替表現になる場合」を中心に返す。単なる形式変更をすべて警告にはしない。
- パース段階の警告と変換段階の警告はUI側で統合する。警告コードは実際に必要になったものだけを追加する。
- 変換形式の選択には `Record<OutputFormat, Converter>` 相当の単純なレジストリを使用し、プラグイン機構は作らない。

## 7. 予定するディレクトリ構成

```text
src/
├─ components/
│  ├─ editor/
│  │  ├─ EditorTabs.vue
│  │  └─ MarkdownEditor.vue
│  ├─ output/
│  │  ├─ MarkdownPreview.vue
│  │  ├─ OutputPanel.vue
│  │  ├─ OutputFormatSelect.vue
│  │  └─ ConversionWarnings.vue
│  └─ common/
│     ├─ AppIcon.vue
│     ├─ AppNotice.vue
│     ├─ IconButton.vue
│     └─ SettingsPopover.vue
├─ composables/
│  ├─ useAppSettings.ts
│  ├─ useClipboard.ts
│  ├─ useDebouncedLocalStorage.ts
│  ├─ useEditorStorage.ts
│  ├─ useEditorTabs.ts
│  ├─ useOutputFormatPreference.ts
│  └─ useThemePreference.ts
├─ converters/
│  ├─ slack/
│  │  ├─ convertToSlack.ts
│  │  └─ convertToSlack.spec.ts
│  ├─ backlog-markdown/
│  │  ├─ convertToBacklogMarkdown.ts
│  │  └─ convertToBacklogMarkdown.spec.ts
│  ├─ backlog-notation/
│  │  ├─ convertToBacklogNotation.ts
│  │  └─ convertToBacklogNotation.spec.ts
│  ├─ plain-text/
│  │  ├─ convertToPlainText.ts
│  │  └─ convertToPlainText.spec.ts
│  ├─ converterRegistry.ts
│  └─ shared.ts
├─ parser/
│  ├─ parseMarkdown.ts
│  ├─ normalizeMarkdownAst.ts
│  └─ parseMarkdown.spec.ts
├─ types/
│  ├─ conversion.ts
│  └─ markdown.ts
├─ App.vue
├─ main.ts
└─ style.css
```

ルートにはVite、TypeScript、Vitest、ESLintの設定と `index.html` を置く。テストは対象コードの近くに配置し、変換例が増えたときに実装と仕様の対応を追いやすくする。

`utils/` は現時点では作成しない。用途の明確でない共通関数置き場を先に設けず、2箇所以上で共通化する具体的な処理が生じた時点で追加する。これは `AGENTS.md` の案からの意図的な差分である。

## 8. Vueコンポーネント構成

### `App.vue`

- Markdown入力、選択中の変換形式、正規化結果、変換結果を結び付ける。
- PCの2カラム配置と狭い画面の入力・出力タブ切り替えを管理する。
- ライト・ダークモードを適用し、選択をLocalStorageから復元する。切り替えは設定ポップオーバー内の太陽・月アイコンボタンで行う。
- ライト・ダークモードに応じたタイトルアイコンをモーダルの開閉ボタンとし、独立した情報アイコンは表示しない。タイトル、アプリの説明文、LocalStorageへの保存と外部送信を行わない旨は押下時のモーダル内に表示する。モーダル表示中の背面操作、Escapeキー、開閉時のフォーカスを管理する。
- ヘッダー左側はフォーカスモード切り替えボタン、タイトルアイコンの順で表示する。ヘッダー右側は設定ボタン、変換形式選択、コピーボタンの順で表示し、変換形式の文字ラベルに代えて変換先アイコンをセレクトボックス内の先頭に表示する。コピー状態と通知もここで管理する。
- 変換ロジックそのものは持たず、parserとConverterを呼び出す。

### `MarkdownEditor.vue`

- 通常の `textarea` と入力文字数を表示する。
- `v-model` 相当の値とイベントに加え、keydownを編集用Composableへ委譲し、保存処理は持たない。
- 太字・斜体・リンク、リスト入力の自動継続、Tabによる階層変更の文字列操作は純粋関数へ分離する。

### `OutputPanel.vue`

- Markdownプレビューと変換結果を切り替える内側タブを持つ。初期表示は変換結果とする。
- 各タブ内にはタブ名と重複する説明見出しを表示しない。
- 変換結果タブに読み取り専用の変換結果、出力文字数、警告一覧を表示する。
- 変換形式選択、コピー処理、通知はヘッダーを管理する `App.vue` に委ねる。

### `MarkdownPreview.vue`

- 既存のMarkdownパーサーと中間表現を利用してHTMLプレビューを表示する。
- 生HTMLは正規化時に破棄し、レンダラーが生成する許可済み要素だけを描画する。
- プレビューのレンダリング処理は純粋関数として単体テスト可能にする。

### `IconButton.vue` / `AppIcon.vue`

- テーマ切り替え、コピー、情報表示で共通するボタン構造と、依存を追加しないインラインSVGアイコンを提供する。
- 操作ごとの状態と処理は各利用元に残し、共通部品へ集約しない。

### `SettingsPopover.vue`

- 設定ボタン、ポップオーバー、テーマのアイコンボタン、エディター内部スクロールのスイッチを表示する。
- 外側クリックとEscapeキーによる閉じる操作を管理し、設定値そのものはpropsとemitで `App.vue` の既存状態へ接続する。

### `OutputFormatSelect.vue`

- MVPの4形式だけを選択肢として表示する。
- 表示名と内部IDの対応を一箇所で管理する。
- セレクトボックスの先頭に既存の共通SVG部品とアイコンボタン部品で変換先アイコンを表示し、セレクトには不可視ラベル、セレクトボックス全体には他のアイコンボタンと同じ共通ツールチップの「変換先」を設定する。ホバーとキーボードフォーカスでツールチップを表示し、アイコンの色は変更せず、アイコン押下時はネイティブのセレクト選択肢を開く。

### `ConversionWarnings.vue`

- `ConversionWarning[]` の総数をボタンへ表示し、同じ警告文は表示時にまとめる。
- ボタンを押したときだけ警告一覧を右ペイン内へ重ねて表示し、左右ペインの高さへ影響させない。
- 警告がない場合はボタンと一覧の領域を表示しない。

### `AppNotice.vue`

- コピー結果など短時間の通知を、通常の画面レイアウトに影響しない固定配置のトーストとして表示する。
- 汎用通知基盤にはせず、MVPで必要な表示だけを扱う。

コンポーネント分割は実装時の責務と規模を見て調整する。小さい部品を機械的に分割するのではなく、変換ロジックとブラウザAPIをUIから切り離すことを優先する。

## 9. LocalStorageへの保存方法

Markdownタブは `useEditorTabs` と `useEditorStorage`、テーマ選択は `useThemePreference`、変換形式は `useOutputFormatPreference`、エディター表示設定は `useAppSettings` Composableが読み書きを担当し、保存と例外処理は共通処理を利用する。

- 保存対象は現在のタブ一覧、選択中のタブID、削除済みタブ、テーマ選択、変換形式、エディター内部スクロール設定とし、入力から再計算できる変換結果は保存しない。
- タブ状態のキーは `markdown-editor-state-v1` とする。タブの表示順は既存の `tabs` 配列順として保存し、専用フィールドや保存形式の版は追加しない。ほかの設定キーは `md-converter:theme:v1`、`md-converter:output-format:v1`、`md-converter:settings:v1` を維持する。
- 初期表示時に一度読み込み、タブ状態がない場合は空の「Untitled」を作成する。既存の `md-converter:draft:v1` だけが存在する場合は、その内容を「Untitled」へ移行する。
- Markdown入力またはテーマ変更後約500msでデバウンスして保存する。タブ追加、切り替え、並べ替え、名称変更、削除、復元、完全削除は直後に保存する。
- 変換形式は操作頻度が低く、変更直後の再読み込みでも選択を失わないよう、変更時に即時保存する。
- エディター内部スクロール設定と左右ペイン比率は変更時に即時保存し、JSONにはbooleanの `editorInternalScroll` と数値の `workspaceSplitRatio` を保持する。旧保存値に比率がない場合は50%へフォールバックする。
- 空文字もタブ内容として保存し、再読み込み後に空の状態を復元する。
- `localStorage` の利用不能、容量超過、セキュリティ例外を捕捉し、入力、設定変更、変換は継続する。保存失敗をユーザーへ通知するかは要確認とする。
- タイマーはコンポーネント破棄時に解除し、必要であれば未保存の最新値を同期保存する。この挙動は単体テストで固定する。
- 保存値は外部へ送信しない。テレメトリや外部フォントなど、入力漏えいにつながり得る外部通信もMVPでは追加しない。

## 10. 単体テストの方針

テストの中心はparser、AST正規化、各Converterとし、Vueコンポーネントではユーザー操作と表示の結線だけを確認する。

### parser・中間表現

- MVP対象の各Markdown要素が期待するノードになること
- 空文字、複数要素、ネストしたリスト、タスクリストを解析できること
- コードブロック内のMarkdown記号を子要素として解釈しないこと
- GFMテーブル（日本語、列幅の異なる文字列を含む）を保持すること
- ソフト改行、明示改行、段落間の空行を区別できること
- 不完全なMarkdownを例外で停止させず、パーサー結果に従って安全に扱うこと

### 各Converter

- `docs/conversion-rules.md` の確定済み例を表形式のテストケースにすること
- 空文字、単一要素、複数要素の組み合わせを確認すること
- 番号付き・箇条書き・チェックリストの入れ子と連続性を確認すること
- インライン装飾の入れ子、リンク内の装飾、コード内の記号を確認すること
- コードブロック、テーブル、水平線、日本語を確認すること
- 出力先で表現できない要素の代替出力と警告を確認すること
- 不完全な構文でも出力が欠落・クラッシュしないこと
- 改行コードをテスト環境に依存させず、出力方針どおりに統一すること

### Composable・UI

- Markdown入力とテーマのLocalStorageからの復元、500msデバウンス、保存失敗時の継続をfake timerとStorageのスタブで確認すること
- タブの先頭・末尾・隣接位置への並べ替え、選択中IDとタブ内容の維持、即時保存・再読み込み後の復元をComposableの単体テストで確認すること
- ドラッグ対象と挿入位置の表示、タブ外ドロップでの取消、名前編集中の無効化、通常クリックによる切り替えをコンポーネントテストで確認すること
- 変換形式の即時保存と復元、未保存・不正値・Storage例外時のSlackへのフォールバックを確認すること
- コピー成功・失敗のメッセージをClipboard APIのスタブで確認すること
- 変換形式の切り替え、文字数表示、警告表示を確認すること
- 狭い画面のタブはDOM上の状態とアクセシビリティ属性を確認し、見た目は手動確認すること
- Markdownショートカット、リスト入力の自動継続、単一・複数行のインデントは、文字列と選択範囲を入出力するUtilityの単体テストで確認すること
- Markdownプレビュー／変換結果タブ、入力・形式選択の状態維持、プレビュー反映、およびヘッダーの形式選択とコピー表示はコンポーネントテストで確認すること
- 文章検索の部分一致、大文字・小文字を区別しない検索、前後移動と循環、空文字・不一致・正規表現特殊文字の文字列検索を純粋UtilityとComposableの単体テストで確認すること
- 現在一致の置換、空文字への置換、全置換、再計算、置換件数、選択中タブだけの更新を単体・画面結合テストで確認すること
- 検索・置換ショートカット、Enter・Shift + Enter、Escape、textareaの選択範囲、フォーカスモード、既存Markdownショートカットとの非競合をコンポーネントテストで確認すること

各変換マイルストーンでは、実装と同時に該当Converterのテストを追加する。仕様変更時には `docs/conversion-rules.md` と対応テストを同じ作業範囲で更新する。最終的に `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build` を成功させる。

## 11. マイルストーンごとの実装範囲

各マイルストーンは前段の完了後に開始し、その段階だけでレビュー・動作確認できる状態にする。後続形式や対象外機能を先行実装しない。

### Milestone 1：プロジェクト初期構成

実施範囲：

- Vue 3 + TypeScript + Viteの最小アプリを構成する。
- npm scripts、TypeScript、Vitest、ESLint、`vue-tsc` の設定を追加する。
- 空に近い `App.vue` と共通スタイルを作成し、開発・テスト・ビルドの基盤だけを整える。
- READMEへセットアップと検証コマンドを記載する。
- 依存パッケージの目的、Node.js対応範囲、バージョンを公式情報で確認する。

対象外：Markdown解析、Converter、完成画面、LocalStorage、コピー処理。

完了条件：

- `npm install` 後に開発サーバーが起動し、最小画面をブラウザで表示できる。
- lint、型チェック、サンプルテスト、本番ビルドがすべて成功する。
- 不要な外部通信やMVP対象外の依存が含まれていない。

### Milestone 2：Markdown解析と共通型

実施範囲：

- `unified`、`remark-parse`、`remark-gfm` の技術検証と導入を行う。
- `MarkdownDocument`、ブロック・インラインノード、変換結果、警告の共通型を定義する。
- Markdown文字列をmdastへ解析し、中間表現へ正規化する。
- MVP全要素、空文字、複合要素、入れ子リスト、コードブロック、テーブル、改行、不完全なMarkdownのparserテストを追加する。
- 仕様未確定部分は出力規則に踏み込まず、構文情報を失わず保持する。

対象外：各変換形式への本変換、UIとの接続。

完了条件：

- MVP対象要素を中間表現として識別でき、コード内記号が誤解析されない。
- parser/正規化の単体テスト、lint、型チェック、ビルドが成功する。
- パーサー選定の検証結果と既知の制約が文書化されている。

### Milestone 3：Slack変換

実施範囲：

- `Converter` インターフェースに従うSlack Converterを実装する。
- 確定済み要素と、公式仕様を確認して合意した見出し、装飾、リスト、引用、コード、リンク、テーブル、水平線、改行を変換する。
- Slackで忠実に表現できない要素に代替表現と警告を追加する。
- 単体テストを追加し、出力を一時的な開発用呼び出しまたはテストでレビュー可能にする。

対象外：Slack API投稿、完成UI、他形式の実装。

完了条件：

- 合意済みのSlack変換規則をすべてテストで確認できる。
- コードブロック内の記号、ネストしたリスト、日本語テーブルを安全に変換できる。
- 情報損失が発生するケースで規定の警告が返る。
- 全自動検証が成功する。

### Milestone 4：Backlog Markdown変換

実施範囲：

- Backlog Markdown Converterを実装する。
- Backlogが受け付けるMarkdown仕様を公式情報と実環境で確認し、標準Markdownとの差分だけを明示的に変換する。
- 表現できない要素の代替表現と警告、単体テストを追加する。

対象外：Backlog API連携、Backlog記法Converter。

完了条件：

- 合意済みの全MVP要素がBacklog Markdown向けに変換される。
- 入力を単純にそのまま返すのではなく、AST経由でコード・改行・未対応要素を安全に処理する。
- 公式仕様との差分と警告ケースがテストで確認できる。
- 全自動検証が成功する。

### Milestone 5：Backlog記法変換

実施範囲：

- Backlog記法 Converterを実装する。
- 公式仕様を確認して合意した見出し、装飾、リスト、引用、コード、リンク、テーブル、水平線、改行を変換する。
- 表現できない要素の代替表現と警告、単体テストを追加する。

対象外：Backlog API連携、未確認記法の推測実装。

完了条件：

- 合意済みの全MVP要素がBacklog記法へ変換される。
- ネスト、コード内記号、日本語テーブル、情報損失時の警告をテストで確認できる。
- Backlog Markdownとは独立したConverterとして保守できる。
- 全自動検証が成功する。

### Milestone 6：プレーンテキスト変換

実施範囲：

- プレーンテキスト Converterを実装する。
- 構造を読み取れる代替記号を用い、装飾記号を除去しつつ本文・URL・コードを保持する。
- 情報損失の警告方針を確定し、単体テストを追加する。

対象外：テキスト整形のユーザー設定、エクスポート機能。

完了条件：

- 合意済みの全MVP要素を読みやすいプレーンテキストへ変換できる。
- リスト階層、リンク先、コード、表のセル内容が意図せず欠落しない。
- 代替表現と警告がテストで確認できる。
- 全自動検証が成功する。

### Milestone 7：エディタ画面

実施範囲：

- PC向け左右2カラムの入力・出力画面を実装する。
- textarea、入力文字数、変換形式選択、リアルタイム変換、読み取り専用出力、出力文字数、警告一覧を接続する。
- ライト・ダークモードを実装する。テーマの初期値・保存範囲は事前確認結果に従う。
- UIと変換処理を分離し、ConverterをVueコンポーネントへ埋め込まない。

対象外：コピー、自動保存、狭い画面向け最終調整。

完了条件：

- 4形式を切り替えると、同じ入力から対応する結果と警告が即時表示される。
- 入出力文字数が合意した仕様どおり動作する。
- キーボード操作とラベル付けを含む基本的なアクセシビリティを手動確認できる。
- コンポーネントテストと全自動検証が成功する。

### Milestone 8：コピー、自動保存、警告表示

実施範囲：

- Clipboard APIによるコピーと、仕様に定めた成功・失敗メッセージを実装する。
- 入力とテーマ選択を約500msデバウンスして別々のLocalStorageキーへ保存し、再読み込み時に復元する。
- Storage/Clipboard APIの失敗時も編集・変換を継続できるようにする。
- 警告は変換結果下部へ件数ボタンとして表示し、同一内容をまとめた一覧を右ペイン内へ重ねて表示する。

対象外：クラウド保存、履歴、複数文書、Slack/Backlogへの直接送信。

完了条件：

- 選択中の形式名を含む成功メッセージと、仕様どおりの失敗メッセージが表示される。
- 入力・テーマ変更後のデバウンス保存と、再読み込み後の復元を自動・手動の両方で確認できる。
- 保存・コピーが拒否された場合も入力内容が画面上から失われない。
- Composable/コンポーネントテストと全自動検証が成功する。

### Milestone 9：レスポンシブ対応と最終テスト

実施範囲：

- 狭い画面で入力と変換結果をタブ切り替えにする。
- PC、タブレット相当、スマートフォン相当の表示を調整する。
- 仕様の変換マトリクスを全形式で回帰テストし、結合操作を手動確認する。
- 外部通信がないこと、MVP対象外機能が混入していないことを確認する。
- READMEと仕様文書を最終挙動に合わせて更新する。

対象外：PWA、オフラインキャッシュ、E2E基盤の新規導入。公開・デプロイ作業は別途指示がある場合に限り、静的ホスティング向けの最小設定と文書化を行う。

完了条件：

- 主要画面幅でレイアウト破綻がなく、狭い画面のタブをキーボードでも操作できる。
- `docs/conversion-rules.md` の確定済み全ケースとAGENTS.md指定の異常・境界ケースがテストされる。
- lint、型チェック、全単体テスト、本番ビルドが成功する。
- コピー、復元、形式切替、警告、ライト・ダークモードを手動確認できる。
- MVP完了条件を満たし、残る既知の制約が文書化されている。

### Milestone 10：Markdownプレビューとエディター編集支援

実施範囲：

- 右ペインへMarkdownプレビュー／変換結果の内側タブを追加し、変換結果を初期表示にする。
- 既存のMarkdown解析と中間表現を再利用する安全なHTMLプレビューを追加し、生HTMLを破棄する。
- タブ内にはタブ名と重複する説明見出しを表示しない。
- 太字・斜体・リンクのキーボードショートカットを追加する。
- リスト入力の自動継続、空リスト項目の終了・階層移動を追加する。
- Tab・Shift+Tabによる単一リスト行および複数行の2スペース単位の階層変更を追加する。
- 文字列編集を純粋関数、DOMのフォーカス・選択復元をComposableへ分離する。
- Utility単体テスト、プレビューレンダラー単体テスト、コンポーネントテストを追加する。

対象外：高機能エディターライブラリ、コードブロック内のTab編集、ショートカット設定UI、変換先の追加。

完了条件：

- 右ペインの2タブを切り替えても入力内容と選択形式が失われず、入力がプレビューと変換結果へ即時反映される。
- Clipboard APIは変換結果の状態値をコピーし、空、成功、失敗の表示要件を満たす。
- `Ctrl` と `Command` のショートカット、IME除外、リスト継続・終了、単一・複数行の階層変更と選択範囲がテストされる。
- 生HTMLと危険なリンクURLがプレビューからDOM実行可能な形で出力されない。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 11：設定ポップオーバーとエディター表示設定

実施範囲：

- 変換形式選択の左隣に既存の共通アイコン部品を使った設定ボタンと、小型の設定ポップオーバーを追加する。
- ポップオーバーを設定ボタンの再押下、外側クリック、Escapeキーで閉じられるようにする。
- テーマ設定は既存の `useThemePreference`、`data-theme`、CSS変数、LocalStorage保存処理を再利用し、設定内の月／太陽アイコンボタンから操作する。ヘッダーには独立したテーマボタンを表示しない。
- エディター内部スクロール設定を追加し、アプリ用の版付きLocalStorageキーへJSONで保存・復元する。
- 内部スクロールONではアプリ全体を動的ビューポート高に収めるFlexレイアウトとし、ヘッダーなどを除いた残りの高さをエディターと右ペインへ配分する。textareaとMarkdownプレビューは配分された領域内で `overflow-y: auto` とする。OFFでは入力・貼り付け・削除・外部更新時にtextareaの `scrollHeight` から高さを再計算し、Markdownプレビューは内容に合わせて自然に伸縮させる。
- 設定保存値の欠損、不正JSON、不正型、LocalStorage例外に対するフォールバックを実装する。
- 設定UI、テーマ同期、保存・復元、textarea伸縮の単体・コンポーネントテストを追加する。

対象外：システムテーマ追従、新しいテーマ管理、UI・状態管理ライブラリ、エディターライブラリ、バックエンド同期。

完了条件：

- 設定ポップオーバー、テーマアイコンボタン、エディター設定スイッチをマウス・キーボードで操作でき、フォーカスを視認できる。
- 既存テーマ経路を重複させず、設定内のテーマアイコンからライト・ダークモードを切り替えられる。
- エディター内部スクロールのON/OFFがエディターとMarkdownプレビューへ即時反映され、OFF時は双方が内容の増減に合わせて伸縮する。
- 設定が再読み込み後も復元され、不正値やStorage例外でも編集・変換を継続できる。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 12：未対応の生HTMLを含む入力の継続変換

実施範囲：

- ブロックHTMLとインラインHTMLの元文字列、ブロック種別、位置情報をアプリ用中間表現へ保持する。
- 生HTMLを例外にせず、4つのConverterで文字列として保持して `unsupported-node` 警告を返す。
- 生HTML以外の変換可能な要素を継続して出力し、文書全体が空になる挙動を解消する。
- Markdownプレビューでは既存の生HTML破棄を維持する。
- UI境界の汎用エラー文言を、入力不備と断定せず予期しないエラーであることが分かる内容へ変更する。
- パーサー、各Converter、画面結合の回帰テストを追加する。

対象外：生HTMLのレンダリング、HTMLの構文解析・サニタイズ、画像・脚注など生HTML以外のMVP対象外要素への対応。

完了条件：

- 生HTMLを含む入力でも、生HTMLの原文と他の変換可能な内容が出力に保持される。
- 生HTMLノードごとに、位置情報を含む具体的な `unsupported-node` 警告が返る。
- プレビューのDOMへ生HTMLが出力されない。
- 生HTMLでは汎用エラーへフォールバックせず、具体的な警告を返す。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 13：選択中の変換形式の保存・復元

実施範囲：

- 選択中の変換形式を専用の版付きLocalStorageキーへ保存し、再読み込み時に復元する。
- 保存可能な形式IDを共通定義から検証し、未保存・不正値・Storage例外時はSlackへフォールバックする。
- 既存の `useDebouncedLocalStorage` を再利用し、変換形式は変更時に即時保存する。
- Composable単体テストと画面結合テストを追加し、READMEと技術解説を実際の保存対象へ合わせる。

対象外：変換結果の保存、形式ごとの個別設定、クラウド同期、複数文書管理。

完了条件：

- 選択した変換形式が再読み込み後も維持され、その形式の変換結果が表示される。
- 保存値が未定義または不正でもSlackで起動し、Storageの読み書きが失敗しても画面上の形式切り替えと変換を継続できる。
- 保存対象とキーがプロダクト仕様、実装計画、README、技術解説で一致する。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 14：キーボードによるMarkdown編集支援の拡張

実施範囲：

- ツールバーなどの追加UIを設けず、textareaのキーボードショートカットとして編集支援を追加する。
- 太字、斜体、取り消し線、インラインコード、リンクの追加・解除、未選択時のカーソル配置、加工後の選択復元を、文字列と選択範囲を入出力する共通Utilityへ集約する。
- リンクと、本文中のバッククォートとの衝突を回避するインラインコードは、共通の編集結果型を利用する専用処理として実装する。
- 箇条書き、番号付きリスト、引用、見出し1を、選択範囲と交差する行へ適用する共通の行加工Utilityとして実装する。
- 全対象行に加工済みの場合は解除し、未加工行が含まれる場合は既存の加工を重複させず未加工行だけへ適用する。
- 既存のリスト継続、Tab・Shift+Tab、IME除外を維持し、すべてのキーボード編集を同じComposableから実行する。
- リスト項目の文章途中でEnterした場合は現在位置で項目を分割し、後半へ同じリスト記号と階層、または番号付きリストの次番号を付ける。

対象外：編集ツールバー、括弧による文章編集支援、ショートカット設定UI、コードブロック内のTab編集、高機能エディターライブラリ。

完了条件：

- 仕様に定めたCtrlとCommandのショートカットで各加工を実行でき、ブラウザ既定動作は処理したキー操作に限って抑止される。
- 装飾の追加・解除、未選択時のカーソル、リンク、バッククォートを含むインラインコードがUtility単体テストで確認される。
- 単一行・複数行・空行・一部加工済み・行末境界に対する行加工と選択範囲がUtility単体テストで確認される。
- ショートカット操作後の値・選択範囲と、行末・文章途中・空項目におけるEnter、IME、Tabの回帰がコンポーネントテストで確認される。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 15：Markdownタブ管理とデータ保存

実施範囲：

- 最大7件のMarkdownタブを追加・切り替えし、タブごとに入力内容と作成・更新日時を保持する。
- 文書タブをMarkdown入力エリアの最上部へ配置し、入力面と連続するブラウザ風のタブUIとする。重複する `Input` と `Markdown入力` の見出しは表示しない。
- タブ追加ボタンは末尾タブの直後へ配置し、新しいタブの追加後もその横へ続けて表示する。
- ダブルクリックから1〜30文字の名称変更を行えるようにし、名称変更用のメニューは設けない。
- 各タブ右端の「×」アイコンから直接削除できるようにし、最後の1件では無効化する。
- 最後の1件を除くタブを確認なしで削除し、成功トーストを表示せず削除済み一覧から元の位置へ戻せるようにする。
- 内容が空文字かつ現在名がデフォルト形式の `Untitled` または `Untitled N`（Nは2以上）であるタブは、削除済み一覧へ保持せず即時に完全削除する。変更前に保存された `文章N` も同じ扱いを維持する。
- 削除済みタブ一覧から復元・完全削除を行い、削除後30日を経過したデータを起動時に破棄する。
- 削除済み一覧をゴミ箱と件数バッジ、復元を反時計回り矢印、完全削除を「×」付きゴミ箱のアイコンで表示し、アクセシブルなラベルとツールチップを付ける。
- 削除済み一覧を開いている状態で一覧外をクリックした場合は閉じ、一覧内の操作では開いた状態を維持する。
- `useEditorTabs` と `useEditorStorage` を追加し、版付きJSONの検証、入力のデバウンス保存、タブ操作の即時保存、破損データからの初期化を実装する。
- 既存の `md-converter:draft:v1` の内容を、初回のみ「Untitled」へ移行する。
- タブUIとComposableの境界・異常系・保存復元をテストする。

対象外：独自の編集履歴、削除確認ダイアログ、複数ウィンドウ同期、競合解決、クラウド同期。タブ並べ替えはMilestone 20で扱う。

完了条件：

- タブ追加、切り替え、名称変更、通知を伴わない削除、削除済み一覧からの復元・完全削除が仕様どおり動作する。
- 7件上限、最後の1件の削除禁止、復元時の上限、名称の空文字・文字数制限がテストされる。
- デフォルト名の空タブだけが即時に完全削除され、内容があるタブ・空白入力・名前変更済みの空タブは復元対象になることがテストされる。
- 正常な保存データと旧単一文書を復元でき、破損データではアプリを停止せず初期状態へ戻る。
- 選択中タブの入力だけがMarkdownプレビュー、変換結果、警告、コピー対象へ反映される。
- 文書タブが入力エリアの先頭に表示され、アクティブタブと入力面の対応を視覚的に判別できる。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 16：左右ペイン幅の調整

実施範囲：

- 768px以上のPC表示で、Markdown入力と右ペインの間にドラッグ可能な区切りバーを追加する。
- ポインター操作中はworkspaceの実幅を基準に比率を計算し、各ペインが280px未満にならない範囲へ制限する。
- 区切りバーをフォーカス可能なseparatorとして実装し、左右矢印キーで5%ずつ変更、Home・Endキーで可動範囲の端へ移動、ダブルクリックで50:50へ戻せるようにする。
- 調整した比率を既存の `useAppSettings` と `md-converter:settings:v1` へ保存し、旧保存値や不正値では50:50へフォールバックする。
- 767px以下では既存の入力／右ペインのタブ切り替えを維持し、区切りバーを表示しない。
- Composableとコンポーネントテストを追加し、ライト・ダークモードおよび内部スクロールON/OFFの既存レイアウトを回帰確認する。

対象外：上下分割、ペインの折りたたみ、複数のレイアウトプリセット、タブごとの比率保存。

完了条件：

- PC表示でポインターとキーボードの両方から左右ペイン比率を調整でき、各ペインの最小幅が維持される。
- 区切りバーがseparatorとして現在値と操作方法を支援技術へ伝え、50:50へ戻せる。
- 比率が再読み込み後も復元され、旧保存値・不正値・Storage例外でも50:50で編集と変換を継続できる。
- 狭い画面のタブ切り替えへ影響せず、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 17：フォーカスモード

実施範囲：

- 通常ヘッダーの左端へフォーカスモード切り替えアイコンを追加し、その右隣へタイトルアイコンを配置する。フォーカスモード中はタイトルアイコンを表示しない。
- フォーカスモード中も既存のMarkdownEditorと同じtextarea DOMを維持し、入力支援、Undo/Redo、自動保存、選択中タブとの接続を継続する。
- 切り替え前後に選択範囲とtextareaのスクロール位置を取得・復元し、切り替え直後はエディターへフォーカスする。
- PCでは左25%、中央50%、右25%の固定グリッドとし、左に終了・解説操作、中央に編集可能なタブ名とtextarea、右に空白を表示する。
- 左の解説欄に実装済みのMarkdownショートカット、自動リスト継続、インデント、IME変換中の制約、コードフェンス内のリスト操作制約を表示し、解説欄だけを独立してスクロール可能にする。
- 767px以下では中央を全幅表示し、終了・解説操作を左上へ固定して、解説欄を左側からのドロワーとして表示する。
- フォーカスモード中だけtextarea内部スクロールを強制し、既存の `editorInternalScroll` 保存値は変更しない。
- タブ名編集を通常タブとフォーカスモードで共通化し、Enter・フォーカス離脱で確定、Escapeでキャンセルする。編集中のEscapeはフォーカスモード終了へ伝播させない。
- 終了ボタンまたはEscapeキーで通常モードへ戻し、終了直前のカーソル、選択範囲、スクロール位置を入力ペインで復元する。
- 通常モードのEscapeキーでもフォーカスモードへ入り、タブ名編集、設定、警告、情報モーダルなどがEscapeキーを処理した場合はモードを切り替えない。
- モード開始ごとに解説欄を閉じ、フォーカスモードと解説欄の状態をLocalStorageへ保存しない。
- コンポーネントテストで表示切り替え、非表示対象、状態保持、Esc制御、タブ名編集、解説欄、再読み込み時の非永続化を確認する。

対象外：ブラウザFull Screen API、別画面への遷移、モーダル型エディター、タブ切り替え・追加・削除、フォーカスモード状態の永続化、タブごとの解説欄状態。

完了条件：

- 通常画面からフォーカスモードへ入り、同じ選択中タブを既存の入力支援と保存処理で編集できる。
- PCの25%／50%／25%固定配置と、767px以下の全幅エディター・左ドロワーが既存ブレークポイントで適用される。
- モード切り替え前後でtextarea DOM、カーソル、選択範囲、スクロール位置が維持され、開始直後に入力できる。
- タブ名編集中のEscapeは名前変更だけを取り消し、それ以外のEscapeと終了ボタンは通常モードへ戻す。
- フォーカスモード開始時に解説欄が閉じ、再読み込み時は通常モードで開始する。
- 通常モードのタブ管理、左右ペイン、設定、変換、コピーへ回帰がなく、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 18：通常モードの入力支援表示

実施範囲：

- 通常モードの入力フッター左側にラベルのない入力支援のアイコンボタンを追加し、操作名は `aria-label` とツールチップで保持する。文字数は右側に維持する。
- ボタンからMarkdown入力欄の下、フッターの上へ固定高の入力支援パネルを開き、入力欄に重ねずに内容を表示する。
- フォーカスモードと通常モードの解説本文を共通コンポーネントへ分離する。
- ボタンの再押下またはEscapeキーで閉じ、入力欄の操作では閉じない。Escapeで閉じた場合はボタンへフォーカスを戻す。
- 入力支援を開いた状態でフォーカスモードへ入る場合は通常モードの入力支援パネルを閉じ、表示状態を保存しない。
- 表示、開閉、共通内容、Escape制御、フォーカスモードへの回帰をコンポーネントテストで確認する。

対象外：入力支援の設定変更、ショートカットのカスタマイズ、表示状態のLocalStorage保存、モーダル表示。

完了条件：

- 通常モードで入力支援を表示しながらエディターを操作でき、狭い画面でもパネル内に収まる。
- 通常モードとフォーカスモードの解説内容が同一コンポーネントから表示される。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 19：共通ツールチップ

実施範囲：

- 操作説明に使用しているブラウザ標準の `title` 属性を、依存を追加しない共通ツールチップへ置き換える。
- ツールチップをホバーとキーボードフォーカスで待ち時間なく表示し、ポインター離脱、フォーカス離脱、Escapeキーで閉じる。
- ツールチップを`body`直下へ表示し、対象要素の位置とビューポートを基準に上下位置と左右端を調整する。
- 共通アイコンボタン、変換先選択、Markdownタブ操作、入力支援、左右ペイン区切りへ同じ表示処理を適用する。
- 既存の `aria-label`、無効状態、クリック、キーボード操作を維持し、ツールチップを操作可能なUIにはしない。
- 表示、動的な文言更新、Escape、位置調整、破棄時の後始末を単体・コンポーネントテストで確認する。

対象外：リッチコンテンツ、ツールチップ内の操作、表示時間のユーザー設定、タッチ操作専用の補足UI、外部UIライブラリ。

完了条件：

- 対象操作のツールチップがホバーとキーボードフォーカスですぐ表示され、ブラウザ標準ツールチップと二重表示されない。
- スクロール領域と画面端の対象でもツールチップがビューポート内に表示される。
- 既存の操作名を維持し、無効なボタンでもホバー時に理由を確認できる。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 20：Markdownタブのドラッグ並べ替え

実施範囲：

- PCでタブ名をブラウザ標準のDrag and Drop APIによりドラッグし、別タブの前後へ並べ替えられるようにする。
- ドラッグ中のタブと、ドロップ先の前後に挿入予定位置を表示する。
- `useEditorTabs` にID基準の並べ替え操作を追加し、タブ配列の順序だけを変更する。タブのID、名称、入力内容、日時、選択中IDは維持する。
- 並べ替え後の `tabs` 配列を `markdown-editor-state-v1` へ即時保存し、既存のversion 1データと保存キーを維持する。
- タブ外へのドロップ、同じ位置への移動、存在しないIDは順序変更なしとして安全に扱う。
- タブ名編集中、削除ボタン、追加ボタン、削除済み一覧からはドラッグを開始しない。ドラッグ直後のクリックと通常クリックを区別する。
- Composableで並べ替え、追加・削除・復元との組み合わせ、保存・再読み込みをテストし、コンポーネントでドラッグ表示、ドロップ確定・取消、編集・クリックとの競合をテストする。

対象外：外部ドラッグライブラリ、キーボードによる並べ替え、タッチ専用の独自ポインター操作、保存形式の新版、複数ウィンドウ同期。

完了条件：

- 先頭から末尾、末尾から先頭、隣接位置への並べ替えができ、選択中タブと各タブの名前・本文の対応が変わらない。
- 並べ替え後の順序が即時保存され、再読み込み後に同じ順序で復元される。
- 追加・削除・復元・名前変更、7件上限、通常クリックによる切り替えに回帰がない。
- タブ外へのドロップとタブ名編集中のドラッグでは順序を変更しない。
- lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 21：文章検索・一括置換

実施範囲：

- 選択中タブのMarkdown本文だけを対象に、大文字・小文字を区別しない部分一致検索を追加する。
- 正規表現を使用せず、検索語に正規表現の特殊文字が含まれる場合も通常文字として扱う純粋Utilityを追加する。改行をまたぐ検索、表記ゆれの吸収、Markdown構文解析は行わない。
- 検索語、置換語、現在位置、検索結果、置換件数を管理するComposableを追加し、本文の直接編集とタブ切り替えで検索結果を再計算する。
- 文書タブとtextareaの間に、最初は検索行だけを表示する検索・置換UIを追加する。通常モードの入力フッターとフォーカスモードの左側操作群に検索アイコンを追加し、必要時だけ置換行を展開する。
- 前へ・次へを循環させ、一致範囲を既存textareaの `selectionStart` と `selectionEnd` で選択して見える位置へ移動する。
- 現在一致の置換と全置換を、選択中タブの既存本文更新・デバウンス保存経路へ接続する。全置換はtextarea全体に対する1回の編集として適用する。
- `Ctrl + F` / `Command + F`、`Ctrl + H` / `Command + Option + F`、Enter、Shift + Enter、Escapeを追加し、ブラウザ検索より優先しながら既存Markdown編集、ポップオーバー、フォーカスモードのEscape制御を維持する。
- 検索、置換、タブ分離、ショートカット、通常・フォーカスモードのコンポーネントテストを追加し、READMEへ機能と操作を記載する。

対象外：正規表現検索、大文字・小文字の切り替え、全角・半角変換、表記ゆれ吸収、改行をまたぐ検索、非表示タブ・タブ名・プレビュー・変換結果の検索、高機能エディター、独自Undo・Redo履歴、検索状態の永続化。

完了条件：

- 選択中タブの本文だけで検索件数と現在位置を表示し、前後移動、先頭・末尾の循環、textareaの範囲選択が動作する。
- 現在一致と全一致を置換でき、再計算、空文字への置換、一致なし、置換件数、他タブの非変更がテストされる。
- 通常モードとフォーカスモードでマウス・キーボードから利用でき、検索中のEscapeがフォーカスモードを切り替えない。
- 既存の本文更新、自動保存、Markdownショートカット、タブ切り替え、ブラウザ標準Undo・Redo経路に回帰がない。
- 外部依存を追加せず、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 22：検索一致箇所のハイライト

実施範囲：

- Milestone 21の検索結果を利用し、検索中の全一致箇所をMarkdown入力欄内でハイライトする。現在の一致は他の一致より強い色で区別する。
- 既存textareaの背面に表示専用のハイライト層を配置し、textarea DOM、入力、選択、IME、スペルチェック、ブラウザ標準Undo・Redoを維持する。
- ハイライト層は本文を安全なテキストとして描画し、ポインター操作とアクセシビリティツリーの対象外にする。
- textareaと同じフォント、行高、余白、折り返しを適用し、内部スクロール時は縦横のスクロール位置を同期する。
- 通常モード、フォーカスモード、ライト・ダークテーマ、内部スクロールON・OFFの既存レイアウトを維持する。
- 検索UIを閉じた場合、検索語が空の場合、一致がない場合はハイライト層を表示しない。
- ハイライトの分割、現在位置の区別、本文更新、前後移動、スクロール同期をコンポーネントテストへ追加する。

対象外：Markdown構文のシンタックスハイライト、検索対象や一致判定の変更、高機能エディター、独自Undo・Redo履歴、ハイライト状態の永続化。

完了条件：

- 全一致と現在の一致を視覚的に区別でき、前後移動と本文編集に追従する。
- textareaの選択範囲、入力、置換、自動保存、通常・フォーカスモードの切り替えに回帰がない。
- 内部スクロール時にハイライト層の縦横位置がtextareaへ追従する。
- 外部依存を追加せず、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 23：入力置換

実施範囲：

- アプリ全体で共通の入力置換設定と最大100件のルールを、専用の版付きLocalStorageキーへ保存・復元する。
- 設定ポップオーバーへ全体ON/OFFと管理ボタンを追加し、専用モーダルで追加、有効化、編集、削除を行う。
- 入力文字と置換後文字をGFMのmdastで検証し、不完全なMarkdown記号は明示的な禁止リストで補完する。
- Markdown本文の既存textareaだけで、直接入力のSpace・全角Space・EnterとIME確定を契機に完全一致ルールを1件だけ適用する。
- mdastのoffsetからインラインコードとコードブロックの範囲を取得し、コード内では置換しない。
- Enterでは置換後の本文に既存リスト継続を適用し、1回のtextarea編集として反映する。
- 貼り付け、ドロップ、復元、タブ切り替え、検索・一括置換、Undo／Redoでは入力置換を実行しない。
- 純粋Utility、保存Composable、管理UI、通常・フォーカスモード、既存入力支援との回帰テストを追加する。
- 詳細は `docs/input-replacement-spec.md` に従う。

対象外：正規表現、部分一致、連続置換、タブごとのルール、並べ替え、インポート・エクスポート、動的文字列、独自Undo／Redo履歴、高機能エディター。

完了条件：

- IME変換中は置換せず、確定後に1回だけ適用され、IME確定Enterで改行・リスト継続が発生しない。
- Space・全角Space・Enter、カーソル位置、最長一致、全体・個別の無効化がテストされる。
- コード内、貼り付け、検索置換、タブ名など対象外の入力では置換されない。
- ルールの検証、100件上限、保存・復元、破損値、Storage例外がテストされる。
- 通常モードとフォーカスモードで同じルールを使用し、既存のショートカット、リスト、Tab、検索、タブ、自動保存、変換に回帰がない。
- 対象ブラウザでIMEとブラウザ標準Undo／Redoを手動確認できる。
- 外部依存を追加せず、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 24：全角Markdown補正

実施範囲：

- 既存の設定ポップオーバーへ初期OFFの「全角Markdown補正」スイッチを追加し、既存の `md-converter:settings:v1` へ保存・復元する。
- 直接入力またはIME確定した全角Markdown記号を1文字から補正し、見出し、リスト、チェックリスト、引用、インライン装飾、コード、リンク、テーブル、水平線を半角Markdownとして完成できる純粋Utilityを追加する。
- 本文全体のNFKC正規化は行わず、通常文章の全角文字、コード本文、貼り付け、検索・一括置換、保存内容の復元、Undo／Redoを補正対象外とする。
- 直接入力、IME確定、Enterの既存入力イベント経路へ補正処理を接続し、入力置換、リスト継続、カーソル復元、ネイティブUndo／Redo経路を維持する。
- 通常モードとフォーカスモードで同じ設定とtextareaを使用し、共通の入力支援ガイドへ挙動を追記する。
- Utility、設定Composable、設定UI、MarkdownEditor、画面結合の回帰テストを追加する。
- 詳細は `docs/full-width-markdown-normalization-spec.md` に従う。

対象外：本文全体の文字種変換、貼り付け内容の一括補正、検索の全角・半角同一視、タブごとの設定、ユーザー定義の補正記号、外部依存、高機能エディター。

完了条件：

- 設定が初期OFFで、ON/OFFが即時反映・保存・復元され、旧保存値・不正値・Storage例外ではOFFへフォールバックする。
- ONの場合だけMVP対象の全角Markdown構文が半角化され、既存Parserで対応するMarkdownノードとして解析される。
- 半角Markdown、通常文章、コード本文、貼り付け、検索置換、復元、Undo／Redoを意図せず変更しない。
- IME、Space、全角Space、Enter、入力置換、リスト継続、通常・フォーカスモードの回帰がテストされる。
- 対象ブラウザでIMEとブラウザ標準Undo／Redoを手動確認できる。
- 外部依存を追加せず、lint、型チェック、全単体テスト、本番ビルドが成功する。

### Milestone 25：変換形式の選択肢順変更

実施範囲：

- 変換形式の選択肢を「Backlog記法」「Backlog Markdown」「Slack」「プレーンテキスト」の順へ変更した。
- 選択肢の値とラベルの表示順を画面結合テストへ追加した。
- 保存形式と初期選択は変更せず、未保存・不正値の場合は従来どおりSlackへフォールバックする設計を維持した。

対象外：変換処理、形式名、保存形式、初期選択、変換形式の追加・削除、その他のUI変更。

完了結果：

- 4つの変換形式が仕様どおりの順序で表示される。
- 既存の保存・復元テストを含む全386件のテストが成功した。
- 依存関係を追加せず、lint、型チェック、本番ビルドが成功した。

### Milestone 26：アプリ名称の変更

実施範囲：

- アプリの表示名を「Markdown Converter」から「Md Converter」へ変更した。
- ブラウザのタイトルと説明、情報モーダル、タイトルアイコンの代替テキストを更新した。
- README、現行仕様、技術解説の名称と、表示名を検証するテストを更新した。

対象外：npmパッケージ名、LocalStorageのキー、ディレクトリ名、タイトルアイコンとfavicon画像、変換仕様、画面機能。

完了結果：

- ユーザーに提示するアプリ名称と現行ドキュメントの名称を「Md Converter」に統一した。
- タイトル画像とfaviconは文字列を含まない既存のシンボルを維持した。
- 全22テストファイル、386件のテストが成功した。
- 依存関係を追加せず、lint、型チェック、本番ビルドが成功した。

### Milestone 27：情報モーダルへのタイトルアイコン追加

実施範囲：

- 情報モーダルの見出し左側に既存のタイトルアイコンを追加した。
- ライト・ダークテーマに応じた画像選択を `AppTitleIcon` へ共通化し、ヘッダーとモーダルで再利用した。
- モーダル内のアイコンは、見出しとの重複読み上げを避けるため装飾画像として扱った。
- ライトテーマでの表示、装飾画像の代替テキスト、ダークテーマへの追従を画面結合テストへ追加した。

対象外：タイトルアイコン画像、favicon、モーダル本文、開閉操作、その他の画面レイアウトと機能。

完了結果：

- 情報モーダルの見出し左側に40pxのタイトルアイコンを表示した。
- 全22テストファイル、387件のテストが成功した。
- 依存関係を追加せず、lint、型チェック、本番ビルドが成功した。

### Milestone 28：情報モーダルの説明更新

実施範囲：

- 情報モーダルの1段落目を、入力支援、検索・置換、プレビューを備えたMarkdownエディターであることと、Backlog、Slack等への変換・コピーという目的が伝わる説明へ変更した。
- LocalStorageへの保存、外部サーバーへ送信しないこと、サイトデータ削除時に保存内容も削除されることを示す2段落目は既存文言を維持した。
- 現行仕様と画面結合テストの期待値を実装と同じ文言へ更新した。

対象外：情報モーダルのレイアウト、開閉操作、アクセシビリティ構造、エディター、プレビュー、変換、保存の各機能、その他の画面文言と変換仕様。

完了結果：

- 情報モーダルに確定した2段落の説明を表示した。
- 全22テストファイル、387件のテストが成功した。
- 依存関係を追加せず、lint、型チェック、本番ビルドが成功した。

### Milestone 29：情報モーダルのタイトル縦配置

実施範囲：

- 情報モーダルのタイトルアイコンを72pxへ拡大し、タイトルの上へ縦並びで中央配置した。
- アイコンとタイトルの間を12px、タイトル領域と本文の間を32pxとして、本文と視覚的に分離した。
- 閉じるボタンの配置基準をタイトル領域からモーダル本体へ変更し、外周から上・右各12pxの位置へ配置した。現行仕様も実装と同じ配置へ更新した。

対象外：タイトルアイコンの画像、情報モーダルの本文と幅、開閉操作、アプリヘッダーとその他の画面レイアウト。

完了結果：

- HTMLの読み上げ順を変えず、CSSだけで導入ページに近いタイトル構成へ変更した。
- 全22テストファイル、387件のテストが成功した。
- 依存関係を追加せず、lint、型チェック、本番ビルドが成功した。

## 12. 技術的なリスク

| リスク | 影響 | 対応方針 |
| --- | --- | --- |
| Slack/Backlogの実際の貼り付け解釈と文書化された記法に差がある | 貼り付け後の表示が期待と異なる | 各Converter着手前に公式仕様を確認し、可能なら対象サービス上で代表ケースを手動検証する |
| CommonMark/GFMと入力者が想定するMarkdownに差がある | 不完全な記法や改行の解釈が異なる | 採用方言をGFMベースと明記し、曖昧ケースをfixture化する |
| mdastから中間表現への正規化で情報を落とす | 後段Converterで正しく出力できない | MVP要素ごとのparserテストを先に作り、位置・改行・リスト情報を必要最小限保持する |
| ネストしたインライン装飾が出力先で表現できない | 記号の衝突や情報損失が起きる | ノードを再帰的に描画し、出力先の制約に応じて警告と安全な代替表現を返す |
| Slack向けテーブルの等幅整形で日本語の表示幅がずれる | コードブロック内の列が揃わない | 文字数と表示幅を混同せず、全角・絵文字の扱いを仕様確認する。必要性が確定するまで幅計算依存は追加しない |
| リアルタイム解析の負荷が大きい入力でUIが遅延する | 入力体験が悪化する | MVPでは同期処理を計測し、問題が確認された場合のみ変換デバウンスや入力上限を検討する。Workerは先行導入しない |
| LocalStorageやClipboard APIが利用できない | 保存・コピーに失敗する | 例外を捕捉し、編集と手動コピーを継続できるメッセージを表示する |
| ブラウザ・OSで改行やClipboard挙動が異なる | 貼り付け結果が不一致になる | 内部改行を統一し、対象ブラウザ範囲を決めて手動確認する |
| 保存されたテーマ値が不正または旧形式である | 意図しないテーマで表示される | `dark` と `light` だけを受け入れ、それ以外はライトモードへフォールバックする |
| 保存された変換形式が不正または廃止済みである | 対応するConverterを取得できない | 共通の形式ID一覧で保存値を検証し、不正値はSlackへフォールバックする |
| パーサー関連依存のESM/Node対応条件 | テスト・ビルド構成が衝突する | Milestone 1/2で公式の対応条件と最小構成を検証し、lockfileで固定する |

## 13. 仕様上の要確認事項

以下は実装前にプロダクト判断または公式仕様・実環境での確認が必要な事項である。解決済みの項目は、確定した方針と参照先を記録する。

### 変換仕様

1. `docs/conversion-rules.md` にない見出し2〜6、段落、箇条書き、番号付きリスト、ネスト、コードブロック、テーブル（Slack以外）、改行の各形式での正確な出力。
2. Slackの対象がメッセージ作成欄への貼り付け用テキストか、APIの `mrkdwn` か。MVPはAPI連携対象外だが、両者の解釈差を混同しない必要がある。
3. Slackの見出し代替、番号付き・ネストリスト、チェック記号、水平線、コードフェンス、リンク構文が現在の公式クライアントで意図どおり扱われるか。
4. Slackテーブルをコードブロック化する際、列をパイプ区切りのまま残すか、表示幅を揃えるか、ヘッダー区切り行を残すか。
5. Backlog MarkdownとBacklog記法それぞれで有効な、見出しレベル、装飾、タスクリスト、引用、コードブロック、リンク、テーブル、水平線、改行の公式記法。
6. Backlogのプロジェクト設定や入力欄によって記法モード・対応要素が異なる場合、どの利用条件を対象とするか。
7. 出力先で直接表現できない「装飾の入れ子」「表内の複数段落や改行」「リスト内コードブロック」などの代替表現と警告要否。
8. （Milestone 6で解決済み、変換規則改定済み）プレーンテキストでは、見出しレベル1を `【見出し文字列】`、レベル2を `■ 見出し文字列`、レベル3を `▼ 見出し文字列`、レベル4〜6を `● 見出し文字列` で表す。レベル4〜6だけは階層の統合を警告する。コードブロックは各行4スペースの字下げ、データ行がある2列テーブルは `項目：値`、データ行がない2列テーブルと3列以上のテーブルはヘッダーを含むタブ区切りで表す。詳細は `docs/conversion-rules.md` の「プレーンテキスト変換の対象と補足」に従う。
9. （Milestone 6のプレーンテキストについて解決済み）ソフト改行と明示的な改行はいずれも改行文字として保持し、段落などのブロック間は空行で区切る。他形式の確定済み規則は `docs/conversion-rules.md` の各節に従う。
10. 不完全なMarkdownをどの範囲で警告するか。CommonMark上は通常テキストになる入力を一律にエラー扱いしない方針でよいか。
11. （Milestone 6のプレーンテキストについて解決済み、変換規則改定済み）警告は情報が実際に欠落する場合だけ返す。同一種類の情報損失は、レベル4〜6の見出し、リンク、チェック項目、コードブロック、テーブルの各出現単位で返し、レベル1〜3の見出し、通常の装飾除去、リスト記号の統一、水平線の代替表現、空行の正規化には返さない。詳細は `docs/conversion-rules.md` に従う。

### UI・保存仕様

12. （Milestone 13で更新済み）初期選択はSlackとする。選択した変換形式は `md-converter:output-format:v1` へ即時保存して再読み込み時に復元し、未保存・不正値・Storage例外時はSlackへフォールバックする。
13. （Milestone 7で解決済み）文字数はUnicodeコードポイント数で数える。UTF-16コード単位ではサロゲートペアを2文字と数えるため、画面上の文字数として採用しない。結合文字や絵文字シーケンスを1文字にまとめる書記素単位の計数は、対応ブラウザが未確定のためMVPでは導入しない。
14. （UI簡素化で更新済み）全削除ボタンは提供しない。
15. （Milestone 8で更新済み）ライト・ダークモードは手動切替とし、選択を `md-converter:theme:v1` へ保存する。保存値がない場合または不正な場合はライトモードを使用する。
16. （Milestone 8で解決済み）LocalStorageへの保存失敗は通知を追加せず、画面上の入力と変換を継続する。保存失敗時の通知文言がプロダクト仕様に定められておらず、コピー通知と誤認させないためである。
17. （Milestone 9で解決済み）767px以下を狭い画面とし、初期タブは入力とする。入力中に表示が意図せず切り替わらないよう、変換後も出力タブへ自動移動しない。タブは左右矢印、Home、Endキーで操作できるようにする。
18. （Milestone 9時点の既知の制約）対応ブラウザの正式な範囲は未確定である。Clipboard API、LocalStorage、CSS Gridおよび標準的なタブ操作を利用できるブラウザを前提とし、対象ブラウザ決定後に実機確認する。
19. （Milestone 10で解決済み）右ペイン内タブの初期表示は変換結果とする。外側の狭い画面向けタブは入力／右ペインの切り替えとして維持し、初期表示は入力のままとする。
20. （Milestone 10で解決済み）選択文字列からリンクを挿入した場合は `URL` を選択する。選択がない場合は `[](URL)` のリンクテキスト位置へカーソルを置く。
21. （Milestone 10で解決済み）プレビューでは生HTMLノードを破棄し、レンダラーが許可したHTMLだけを生成する。リンクは相対URL、フラグメント、`http`、`https`、`mailto`、`tel` だけを有効にする。

## Milestone 1で実施する具体的な作業

次回Milestone 1を開始する場合は、次の順序で進める。

1. Node.js/npmの対象バージョンと、Vue/Vite/Vitest/ESLint/vue-tscの公式な互換条件を確認する。
2. 既存ファイルを保持したまま、Vite + Vue 3 + TypeScriptの最小ファイル群を作成する。
3. `package.json` に開発、ビルド、テスト、lint、型チェックのscriptsと必要最小限の依存を定義し、lockfileを生成する。
4. TypeScript、Vite、Vitest、ESLintの最小設定を追加する。
5. `main.ts`、最小の `App.vue`、グローバルスタイル、`index.html` を追加する。変換UIはまだ作らない。
6. テスト基盤確認用の小さなサンプルテストを追加する。
7. READMEに必要環境、セットアップ、各検証コマンドを記載する。
8. 開発表示を手動確認し、lint、型チェック、単体テスト、本番ビルドを実行して結果を報告する。

Milestone 1ではMarkdownパーサーをまだ追加せず、パーサー導入と中間表現はMilestone 2に限定する。
