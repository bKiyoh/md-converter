# Md Converter リリース後修正・改善計画

> 文書区分：リリース後の現行計画。製品仕様の正本ではない。

## 現在の状態

- 基準日：2026-08-12
- 製品フェーズ：初回リリース後の保守
- アクティブなマイルストーン：PR-1 変換結果の情報保持
- 次のマイルストーン：PR-1完了後に候補一覧から1件を選定する

この計画はリリース後の調査で確認した項目を起点とする。初回リリース前の計画と
完了済み作業は、それぞれ `../plans/implementation-plan.md` と
`../archive/implementation-plan-completed.md` を参照する。

## 修正候補一覧

| ID | 優先度 | 区分 | 確認した内容 | 状態 |
| --- | --- | --- | --- | --- |
| PR-1A | High | 変換不具合 | Slack向けインラインコードとコードブロックが固定長のバッククォートを使い、本文中の同じ区切り文字と衝突する | アクティブ |
| PR-1B | High | 変換不具合 | Backlog Markdownの表セル内にあるインラインコードの `\|` が再エスケープされず、列構造を壊す可能性がある | アクティブ |
| PR-1C | Medium | 仕様判断を伴う不具合 | 脚注参照は警告へ変換されるが、脚注定義の本文がParser正規化時に失われる | アクティブ |
| PR-2 | High | リリース品質 | 依存監査にHigh脆弱性が残り、インストール済み依存とlockfileの不一致によりライセンス検証も失敗する | 未着手 |
| PR-3 | Medium | テスト安定性 | 検索ハイライト上限の単体テストが標準の5秒タイムアウトを超えることがある | 未着手 |
| PR-4 | Medium | 動作確認 | Safari、Firefox、モバイル、IME、Clipboardなどの手動確認が未完了 | 未着手 |
| PR-5 | Medium | 製品リスク | 複数ウィンドウで同時編集するとLocalStorageの後勝ちで変更を上書きする可能性がある | 機能候補 |

優先度は利用者データまたは変換結果への影響を基準にした暫定値とする。着手時に
再現性と影響範囲を再確認し、確認できない項目を推測で修正しない。

## アクティブなマイルストーン：PR-1 変換結果の情報保持

### 目的

対応済みMarkdownを変換した際に、区切り文字の衝突、表構造の破損、脚注本文の欠落で
利用者の情報が失われる問題を解消する。

### 対象範囲

- Slackのインラインコードとコードブロックについて、内容と衝突しない表現または
  意味を維持するフォールバックを定義する
- Slackでリンクタイトルなどを表現できない場合の警告方針を再確認する
- Backlog Markdownの表セル内にあるインラインコードのパイプを安全に出力する
- 脚注参照と脚注定義を一組として扱い、定義本文を失わない代替表現と警告を定義する
- 対応する変換仕様、Parser・Converterの回帰テスト、実装を更新する

SlackとBacklog固有記法は、実装前に各サービスの公式仕様と再照合する。現行仕様と
公式仕様に差異がある場合は、コードより先に差異と影響範囲を報告する。

### 対象外

- PR-2以降の候補
- 新しい出力形式、外部APIへの投稿、サーバー保存
- 変換画面のUI刷新
- 脚注記法をネイティブ対応していない変換先への独自記法追加
- 関連しないParser・Converterのリファクタリング

### 実施順序

1. 最小入力で各問題を再現し、現行仕様との一致または差異を記録する
2. `../specs/conversion-rules.md` にフォールバック、情報保持、警告内容を定義する
3. 失敗する回帰テストをParserと対象Converterへ追加する
4. 仕様で定義した最小範囲だけを実装する
5. lint、型チェック、単体テスト、ビルドを実行する
6. SlackとBacklogの実画面へ貼り付け、表示結果と警告内容を確認する

### 必要なテスト

- 1個以上のバッククォートを含むSlackインラインコード
- 3個以上の連続バッククォートを含むSlackコードブロック
- タイトル付きリンクなど、Slackで情報を保持できない入力の警告
- パイプを含むインラインコードを置いたBacklog Markdown表
- 脚注参照が1件・複数件・参照なし定義の場合の本文保持と警告
- 日本語、改行、他のインライン要素を組み合わせた回帰ケース

### 完了条件

- PR-1AからPR-1Cの再現入力で、本文または表構造が暗黙に失われない
- 変換先で直接表現できない情報は、代替テキストまたは警告で利用者に示される
- 変換仕様とテストが実装結果に一致している
- `npm run lint`、`npm run type-check`、`npm run test`、`npm run build` が成功する
- SlackとBacklogの手動確認結果、未確認範囲が記録されている

## 次のマイルストーン候補

以下はPR-1の対象外であり、同時に実装しない。PR-1完了後に、影響度、再現性、依存関係を
確認して1件だけをアクティブにする。

1. PR-2：lockfileを基準に依存を再構築し、脆弱性とライセンス検証の失敗を解消する
2. PR-3：実利用時の検索性能を計測し、処理改善または妥当なテスト条件へ修正する
3. PR-4：`../release/browser-verification.md` の未確認項目を実機で確認する
4. PR-5：複数ウィンドウの競合検知とローカルバックアップの製品仕様を検討する

## 追加機能の提案

次の項目は不具合修正ではなく、新しい製品判断を要する候補である。採用する場合は
`../specs/product-spec.md` または対象の機能別仕様を更新し、独立したマイルストーンを
定義してから実装する。

- ローカルバックアップのエクスポート・インポート：ブラウザデータ消去や端末移行に備える
- 複数ウィンドウの競合通知：別ウィンドウの更新を検知し、上書き前に選択肢を示す
- 変換結果のファイル保存：Clipboardが使用できない環境でも結果を持ち出せるようにする

